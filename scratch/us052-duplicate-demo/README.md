# US-052 Duplication Demo

This tiny repository exists only to test CodeLens US-052.

Zip the `us052-duplicate-demo` folder and upload it through CodeLens. After indexing and embeddings finish, open the repo's **Issues** tab and look for the **Duplication** section.

The intentionally similar files are:

- `src/orderTotals.js`
- `src/invoiceTotals.js`

